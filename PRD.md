# Class Group Manager - PRD (Product Requirements Document)

---

## 1. 프로젝트 개요

### 1.1 프로젝트명
**Class Group Manager (학급 모둠 관리 웹앱)**

### 1.2 목표
초등학교 교실에서 모둠 활동을 효율적으로 운영하기 위한 올인원 웹앱
- 랜덤 모둠 구성으로 **공정한 팀 편성**
- 타이머로 **활동 시간 관리**
- 칭찬 쿠키로 **긍정적 동기 부여**

### 1.3 타겟 사용자
- 초등학교 교사 (체육, 일반 교과 모두)
- 교실 TV/프로젝터에 띄워서 학생들과 함께 사용

### 1.4 사용 환경
| 환경 | 설명 |
|------|------|
| 대화면 | 교실 TV/프로젝터에 띄워서 학생들에게 표시 |
| 조작 기기 | 교사 노트북/태블릿에서 컨트롤 |
| 네트워크 | 오프라인에서도 동작 (localStorage 기반) |

---

## 2. 기술 스택

| 구분 | 기술 | 선택 이유 |
|------|------|----------|
| 언어 | Vanilla JavaScript | 빌드 없이 바로 실행, 단순함 |
| 스타일 | Tailwind CSS (CDN) | 빠른 개발, 반응형 지원 |
| 데이터 저장 | localStorage | 서버 없이 브라우저에 영구 저장 |
| 빌드 도구 | 없음 | index.html 열면 바로 실행 |

---

## 3. 핵심 기능

### 3.1 학급/학생 명단 관리
- 학급 생성/수정/삭제
- 학급별 학생 명단 저장
- 학급 선택 드롭다운으로 빠른 전환

### 3.2 랜덤 모둠 뽑기
- **n명씩 K개 모둠** 설정
- Fisher-Yates 셔플 알고리즘으로 공정한 랜덤
- **남는 학생 처리 옵션**:
  - 기존 모둠에 분배
  - 별도 소모둠 생성
  - 경고 후 사용자 선택

### 3.3 타이머
- **전체 공용 타이머**: 모든 모둠 동일 시간
- **모둠별 개별 타이머**: 각 모둠 독립 타이머
- 시간 프리셋 (1분, 3분, 5분, 10분, 직접 입력)
- **알림 설정**: 소리+화면 / 화면만 선택 가능

### 3.4 칭찬 쿠키 시스템
- 모둠별 쿠키 부여 (+) / 차감 (-)
- **일회성 모드**: 해당 수업만 사용, 종료 시 초기화
- **누적 모드**: 학급별 쿠키 히스토리 저장
- 쿠키 부여 시 시각적 피드백 (애니메이션)

---

## 4. 구현 Phase

### Phase 1: 기본 골격 (프로토타입)
| 순서 | 기능 | 설명 |
|------|------|------|
| 1 | 프로젝트 셋업 | 폴더 구조, index.html, Tailwind 연결 |
| 2 | 학급 명단 관리 | 입력/저장/불러오기/삭제 |
| 3 | 기본 모둠 뽑기 | n명씩 K개 모둠 랜덤 구성 |

### Phase 2: 핵심 기능 완성
| 순서 | 기능 | 설명 |
|------|------|------|
| 4 | 남는 학생 처리 | 3가지 옵션 제공 |
| 5 | 전체 공용 타이머 | 시작/정지/리셋, 알림 |
| 6 | 칭찬 쿠키 (일회성) | +/- 버튼, 실시간 표시 |

### Phase 3: 확장 기능
| 순서 | 기능 | 설명 |
|------|------|------|
| 7 | 모둠별 개별 타이머 | 각 모둠 독립 타이머 |
| 8 | 칭찬 쿠키 (누적) | 히스토리 저장, 통계 |
| 9 | UI 다듬기 | 대화면 최적화, 터치 친화적 |

### Phase 4: 추후 추가 (선택)
| 기능 | 설명 |
|------|------|
| 뽑기 애니메이션 | 룰렛/슬롯머신 스타일 연출 |
| CSV 가져오기 | 엑셀 명단 일괄 업로드 |
| 결과 이미지 저장 | 모둠 구성 결과 캡처 |
| 조건부 모둠 | 특정 학생 같은/다른 모둠 설정 |
| 다크모드 | 눈 편한 어두운 테마 |

---

## 5. 파일 구조

```
Desktop/class-group-manager/
├── index.html          # 메인 HTML
├── PRD.md              # 이 문서
├── css/
│   └── style.css       # 커스텀 스타일 (애니메이션 등)
├── js/
│   ├── app.js          # 앱 초기화 & 이벤트
│   ├── store.js        # 상태 관리 & localStorage
│   ├── classManager.js # 학급/학생 CRUD
│   ├── groupPicker.js  # 모둠 랜덤 뽑기 로직
│   ├── timer.js        # 타이머 기능
│   └── cookie.js       # 칭찬 쿠키 시스템
└── README.md
```

---

## 6. 화면 구성

### 메인 화면 (단일 페이지)
```
┌─────────────────────────────────────────┐
│  [학급 선택 ▼]  [명단 편집]  [설정]     │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  모둠 설정: [4]명씩 [6]모둠     │   │
│  │  남는 학생: ○분배 ○별도 ○선택  │   │
│  │        [ 🎲 모둠 뽑기 ]         │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌────────┐ ┌────────┐ ┌────────┐      │
│  │ 1모둠  │ │ 2모둠  │ │ 3모둠  │ ... │
│  │ 김OO   │ │ 이OO   │ │ 박OO   │      │
│  │ 최OO   │ │ 정OO   │ │ 한OO   │      │
│  │ 🍪 3   │ │ 🍪 5   │ │ 🍪 2   │      │
│  │[+][-] │ │[+][-] │ │[+][-] │      │
│  └────────┘ └────────┘ └────────┘      │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  ⏱️ 05:00  [시작] [리셋]        │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## 7. 데이터 구조

### localStorage 키
```javascript
// 학급 목록
'cgm_classes': [
  { id, name, students: ['김OO', '이OO', ...], createdAt }
]

// 현재 모둠 상태
'cgm_current_groups': [
  { id: 1, members: ['김OO', '이OO'], cookies: 3 }
]

// 쿠키 누적 기록 (누적 모드용)
'cgm_cookie_history': [
  { date, classId, groups: [...], totalCookies: {...} }
]

// 설정
'cgm_settings': {
  cookieMode: 'session' | 'cumulative',
  timerMode: 'global' | 'perGroup',
  defaultTime: 300,
  timerAlert: 'soundAndVisual' | 'visualOnly'
}
```

---

## 8. 핵심 로직

### 랜덤 모둠 뽑기
```javascript
function pickGroups(students, groupSize, groupCount) {
  const shuffled = [...students].sort(() => Math.random() - 0.5);
  const groups = [];

  for (let i = 0; i < groupCount; i++) {
    groups.push({
      id: i + 1,
      members: shuffled.splice(0, groupSize),
      cookies: 0
    });
  }

  // 남는 학생 처리
  const remaining = shuffled;
  return { groups, remaining };
}
```

### 남는 학생 처리 옵션
1. **기존 모둠에 분배**: 랜덤하게 각 모둠에 1명씩 추가
2. **별도 소모둠**: 남는 학생끼리 새 모둠 생성
3. **선택하게**: 경고 표시 후 사용자가 처리 방법 선택

---

## 9. UI/UX 고려사항

### 대화면 최적화
- 큰 폰트 (모둠 이름, 학생 이름)
- 높은 대비 색상
- 쿠키 애니메이션 (부여 시 시각적 피드백)
- 타이머 큰 숫자 표시

### 조작 편의성
- 쿠키 버튼 크게 (터치 친화적)
- 키보드 단축키 (스페이스: 타이머 시작/정지)
- 모둠 다시 뽑기 확인 모달

---

## 10. 요구사항 요약

| 기능 | 세부 사항 |
|------|----------|
| 학생 명단 | 학급별 저장/불러오기 (localStorage) |
| 모둠 뽑기 | n명씩 K개 모둠, 남는 학생은 경고 후 선택 |
| 뽑기 연출 | 프로토타입은 즉시 표시, 추후 애니메이션 추가 |
| 타이머 | 전체 공용 + 모둠별 개별 둘 다 지원 |
| 타이머 알림 | 소리+화면 / 화면만 선택 가능 |
| 칭찬 쿠키 | +/- 버튼, 일회성/누적 모드 둘 다 지원 |
| 사용 환경 | TV 대화면 + 교사 기기 조작 모두 고려 |

---

## 11. 추후 확장 가능 (Phase 4+)

| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 뽑기 애니메이션 | 룰렛/슬롯머신 스타일 연출 | 높음 |
| CSV 가져오기 | 엑셀 명단 일괄 업로드 | 중간 |
| 결과 이미지 저장 | 모둠 구성 결과 캡처 | 중간 |
| 조건부 모둠 | 특정 학생 같은/다른 모둠 설정 | 낮음 |
| 다크모드 | 눈 편한 어두운 테마 | 낮음 |

---

*작성일: 2025-11-28*
*버전: 1.0*
